{
  "name": "arweave-skill",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir skills/arweave --no-splitting --minify"
  },
  "dependencies": {
    "@ar.io/sdk": "^3.22.1",
    "@permaweb/aoconnect": "^0.0.68",
    "@permaweb/arx": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
